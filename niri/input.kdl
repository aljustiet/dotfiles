input {
    keyboard {
        repeat-delay 600
        repeat-rate 50
        xkb {
            layout "us,ru(mac)"
        }
        numlock
    }

    touchpad {
        dwt
        scroll-factor 0.2
        natural-scroll
        accel-speed 0.0
        accel-profile "flat"
    }

    mouse {
        natural-scroll
    }

    trackpoint {
        natural-scroll
        accel-speed 0.0
        accel-profile "flat"
    }

    focus-follows-mouse max-scroll-amount="0%"
}
